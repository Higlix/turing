3.0 +. 0.01;; (* -> (3.01) *)

2e-4 *. 0.3;; (* -> (2e-05) *)

2 + 3.0;; (* -> (TYPE ERROR) *) (* You can't add a int to a float *)

4 *. 56;; (* -> (TYPE ERROR) *) (* You can't use float operation between ints *)